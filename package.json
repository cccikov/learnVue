{
  "name": "learnvue",
  "version": "0.0.1",
  "description": "this is a project for learning vue",
  "main": "index.js",
  "scripts": {
	"test": "echo \"Error: no test specified\" && exit 1",
	"start":"browser-sync start --server --files \"web/**/*.html,web/**/*.css,web/**/*.js\" --startPath \"web\" --index \"index.html\"",
	"router":"browser-sync start --server --files \"web/**/*.html,web/**/*.css,web/**/*.js\" --startPath \"web/vue-router\" --index \"index.html\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.12",
    "gulp": "^3.9.1",
    "gulp-less": "^3.3.2"
  }
}
